javac -d classes "src/main/java/org/example/*.java"

Manifest-Version: 1.0
Main-Class: org.example.Main
--BLANK

jar cmf MANIFEST.txt APP.jar -C classes .
#create use my custom manifest file redirect jar to my custom path

keytool -genkeypair -alias myKey -keyalg RSA 
-keysize 2048 -validity 365 -keystore dist.jks

jarsigner -keystore dist.jks APP.jar myKey

jarsigner -verify APP.jar